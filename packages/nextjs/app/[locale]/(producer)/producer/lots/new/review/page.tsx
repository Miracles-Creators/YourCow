import type { Metadata } from "next";
import { CreateLotReviewSubmitScreen } from "../../../../_components";

export const metadata: Metadata = {
  title: "Create Lot – Review",
};

export default function CreateLotReviewSubmitPage() {
  return <CreateLotReviewSubmitScreen />;
}
