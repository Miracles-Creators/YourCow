import type { Metadata } from "next";
import { SaleSubmittedScreen } from "../../../../../_components";

export const metadata: Metadata = {
  title: "Sale Submitted",
};

export default function SaleSubmittedPage() {
  return <SaleSubmittedScreen />;
}
