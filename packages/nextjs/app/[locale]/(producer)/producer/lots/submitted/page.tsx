import type { Metadata } from "next";
import { LotSubmittedScreen } from "../../../_components";

export const metadata: Metadata = {
  title: "Lot Submitted",
};

export default function LotSubmittedPage() {
  return <LotSubmittedScreen />;
}
